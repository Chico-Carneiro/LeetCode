/**
 * @param {number[][]} grid
 * @return {number}
 */
var minDays = function(grid) {
    // output is either 0, 1 or 2.

    // check if there's exactly 1 island
      // break if there's 2, return 0
    // if there's exactly 1 island
      // if its topology allows for 1 removal
        // return 1
      // if not, return 2

      
};
